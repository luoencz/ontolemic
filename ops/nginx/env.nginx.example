# Example env for inner-cosmos Nginx template
CANONICAL_HOST=home.xn--the-2na.space
PUNYCODE_CERT_DOMAIN=xn--the-2na.space
PLAIN_CERT_DOMAIN=the-o.space
STATIC_ROOT=/opt/sites/inner-cosmos/frontend/dist
API_UPSTREAM=http://127.0.0.1:8000

# Optional: override output file name (defaults to CANONICAL_HOST)
# SITE_CONF_NAME=inner-cosmos.the-o.space

